/* ============================================================
   PROJECT ORDER
   ------------------------------------------------------------
   This list controls which projects appear and in what order,
   on both the home page and everywhere else.

   To ADD a project:
     1. Copy an existing folder in /content (e.g. custom-macro-pad)
        and rename it to your new project's slug (lowercase-with-dashes).
     2. Edit the project.js inside it.
     3. Add the slug to the list below.

   To REMOVE a project: delete its line below.
   To REORDER: change the order of the lines below.
   ============================================================ */
window.PORTFOLIO_ORDER = [
  "fiber-reinforced-printing",
  "continuous-fiber-retrofit",
  "parol6-robotic-arm",
  "naca-4-digit-airfoil-generator",
  "automated-cnc-part-extraction",
  "custom-slitting-saw-arbor",
  "custom-macro-pad"
];
